# Backend dependencies

# Modal for serverless deployment
modal>=0.60.0

# FastAPI for WebSocket gateway
fastapi>=0.109.0
uvicorn>=0.27.0
websockets>=12.0

# Database (Supabase/Postgres)
supabase>=2.0.0
asyncpg>=0.29.0

# OpenAI for VLM (GPT-4o Vision)
openai>=1.12.0

# Anthropic for Coordinator Agent
anthropic>=0.18.0

# Langfuse for observability
langfuse>=2.0.0

# Dedalus SDK for orchestration
# TODO: Add Dedalus package when available
# dedalus-sdk>=0.1.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
aiohttp>=3.9.0
